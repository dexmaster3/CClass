==========short answers================
1.
a: long amounts[8];

b: float net_inc[12];

c: int daily_tot[12];

d: char n1[50], n2[50], n3[50]...;
   char * names[12] = {n1, n2, n3...};

2.
a: invalid
b: invalid
c: valid
d: invalid
e: valid

3.
char * strcpy(char * new, char * old)
{
    int count, size;
    size = strlen(old);

    for (count = 0; count <= size; count++)
    {
        new[count] = old[count];
    }
    return new;
}
